import '../styles/globals.css'
import type { AppProps } from 'next/app'
import React from 'react'
import { ChakraProvider, extendTheme } from '@chakra-ui/react'
import Head from 'next/head'
import Navbar from '@components/Navbar'
import Footer from '@components/Footer'
import { useEffect, useState } from 'react'

// import { mode } from "@chakra-ui/theme-tools";
import { Provider as WagmiProvider } from 'wagmi'
import { WagmiClient } from '../components/utils/wagmiClient'
// import NavBar from "@components/NavBar";
// import Footer from "@components/Footer";

const theme = extendTheme({
  styles: {
    global: {
      'html,body': {
        backgroundColor: '#000113',
      },
      '*': {
        fontFamily: 'DM Sans',
        color: 'white',
        backgroundColor: 'transparent',
      },
      button: {
        backgroundColor: 'transparent !important',
        border: '1px solid gray',
        _hover: {
          border: '1px solid lightgray',
        },
      },
    },
  },
})
const { ethers } = require('ethers')

export const MyAppContext = React.createContext({
  account: undefined,
  setAccount: undefined,
  myNfts: undefined,
  setMyNfts: undefined,
  contract: undefined,
  setContract: undefined,
  contractUserNFTS: undefined,
  setContractUserNFTS: undefined,
  provider: undefined,
  setProvider: undefined,
  signer: undefined,
  setSigner: undefined,
  allTasks: undefined,
  setAllTasks: undefined,
  selectedTask: undefined,
  setSelectedTask: undefined,
  isQuestSuccessfullycompleted: undefined,
  setIsQuestSuccessfullycompleted: undefined,
  userUD: undefined,
  setUserUD: undefined,
  currentAccountUd: undefined,
  setCurrentAccountUd: undefined,
  myGaslessWallet: undefined,
  setMyGaslessWallet: undefined,
})

export default function App({ Component, pageProps, router }: AppProps) {
  const [mounted, setMounted] = useState(false)
  const [currentAccountUd, setCurrentAccountUd] = useState('')
  const [account, setAccount] = useState('')
  const [myNfts, setMyNfts] = useState([])
  const [contract, setContract] = useState('')
  const [contractUserNFTS, setContractUserNFTS] = useState('')
  const [provider, setProvider] = useState('')
  const [signer, setSigner] = useState('')
  const [allTasks, setAllTasks] = useState('')
  const [selectedTask, setSelectedTask] = useState('')
  const [userUD, setUserUD] = useState('')
  const [myGaslessWallet, setMyGaslessWallet] = useState('')
  const [
    isQuestSuccessfullycompleted,
    setIsQuestSuccessfullycompleted,
  ] = useState(false)

  useEffect(() => setMounted(true), [])
  if (!mounted) return null
  return (
    <ChakraProvider theme={theme}>
      <WagmiProvider client={WagmiClient}>
        <MyAppContext.Provider
          value={{
            account,
            setAccount,
            contract,
            setContract,
            provider,
            setProvider,
            signer,
            setSigner,
            allTasks,
            setAllTasks,
            selectedTask,
            setSelectedTask,
            isQuestSuccessfullycompleted,
            setIsQuestSuccessfullycompleted,
            userUD,
            setUserUD,
            currentAccountUd,
            setCurrentAccountUd,
            myGaslessWallet,
            setMyGaslessWallet,
            contractUserNFTS,
            setContractUserNFTS,
            myNfts,
            setMyNfts,
          }}
        >
          <Head>
            <title>Learning rewards: Tasks</title>
            <meta name="description" content="Generated by create next app" />
            <link rel="icon" href="/favicon.ico" />
          </Head>

          <Navbar account={account} setAccount={setAccount} userUD={userUD} />
          <Component {...pageProps} key={router} />
          <Footer />
        </MyAppContext.Provider>
      </WagmiProvider>
    </ChakraProvider>
  )
}
